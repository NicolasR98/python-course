def cap_text(text: str) -> str:
    """Capitalize each letter of a word

    Args:
        text (str): text

    Returns:
        text: text with all capitalized words
    """
    # return text.capitalize()  If uncomment this line, the test will fail
    return text.title()